.d-sm-flex.align-items-center.justify-content-between.mb-4
  %h3.h3.mb-0.text-gray-800
    = @quiz.title
#hideButton
  = link_to "Start Quiz", question_path(@quiz.questions.first), remote: true, id: "startquiz", class: "d-none d-sm-inline-block btn btn-lg btn-primary shadow-sm"
.question#questionForm

.submit#note
  %h1 Submit Quiz
  %p Click on Submit Quiz button to submit the quiz
  = button_to "Submit Quiz", results_path(@result, {result: {quiz_id: @quiz.id, user_id: current_user.id, status: "Submitted"}}), method: :post, class: "d-none d-sm-inline-block btn btn-md btn-primary shadow-sm"