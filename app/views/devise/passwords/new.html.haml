

/ Outer Row
.row.justify-content-center
  .col-xl-10.col-lg-12.col-md-9
    .card.o-hidden.border-0.shadow-lg.my-5
      .card-body.p-0
        / Nested Row within Card Body
        .row
          .col-lg-6.d-none.d-lg-block.bg-password-image
          .col-lg-6
            .p-5
              .text-center
                %h3.h3.text-gray-900.mb-2 Forgot Your Password?
                %p.mb-4 We get it, stuff happens. Just enter your email address below and we'll send you a link to reset your password!
                = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: {class: "user", method: :post }) do |f|
                  = f.error_notification
                  .form-inputs
                    .form-group
                      = f.text_field :login, required: true, autofocus: true, label: false, placeholder: "Enter Email Address...", class: "form-control form-control-user form-control-range", input_html: { autocomplete: "email" }
                  .form-actions
                    .form-group
                      = f.button :submit, "Send me reset password instructions", class: "btn btn-primary btn-user btn-block"
                  %hr/
                  .text-center
                    = render "devise/shared/links"
